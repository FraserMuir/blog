---
const { src, alt, height, width, aspectRatio, decoding = "async", loading = "lazy" } = Astro.props;
---

<img
  src={src}
  alt={alt}
  width={width}
  height={height}
  decoding={decoding}
  loading={loading}
  style={{ aspectRatio, minWidth: width ?? "", minHeight: height ?? "" }}
/>
